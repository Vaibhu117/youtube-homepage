{
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "tailwindcss": "^3.4.1"
  },
    "scripts": {
        "build": "tailwindcss build -i ./src/input.css -o dist/style.css",
        "deploy": "gh-pages -d dist",
        "predeploy": "npm run build"
       
      }
}

    

